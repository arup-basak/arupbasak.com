generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      =  "file:./db.sqlite3"
}

model User {
  id      Int      @id @default(autoincrement())
  innerData   String   @unique
  name    String?
  time  String
}